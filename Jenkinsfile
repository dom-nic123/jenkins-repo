pipeline {
	agent any
	stage('Build') {
	   steps {
		echo "This should run on the build stage"
	}
     }
       stage ('Test') {
	steps {
		echo "test step"
		}
	}
	stage ('Develop') {
	steps {
		echo "develop step"
		}
	}
}
